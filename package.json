{
  "name": "gpt-clone",
  "author": "Gabriel Cassim",
  "workspaces": [
    "gpt",
    "playground-node"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "playground-node": "npm run start -w @playground/node"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}